prefix="/usr/local"

if [ "$PREFIX" != "" ] ; then
  prefix=$PREFIX
elif [ "$BOXEN_HOME" != "" ] ; then
  prefix=$BOXEN_HOME
fi

rm -rf $prefix/bin/git-media*
for g in git*; do
  cp $g "$prefix/bin/$g"
done

git media init
