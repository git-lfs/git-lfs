machine:
  environment:
    GIT_LFS_TEST_DIR: $HOME/git-lfs-tests
    GIT_SOURCE_REPO: https://github.com/git/git.git
    GIT_EARLIEST_SUPPORTED_VERSION: v1.8.5
    GIT_LATEST_SOURCE_BRANCH: master
    XCODE_SCHEME: test
    XCODE_WORKSPACE: test
    XCODE_PROJECT: test
    GOPATH: $HOME/go

general:
  build_dir: .go/src/github.com/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME

dependencies:
  pre:
    - mkdir -p ./go/src/github.com/$CIRCLE_PROJECT_USERNAME
    - ln -s ~/$CIRCLE_PROJECT_REPONAME ./go/src/github.com/$CIRCLE_PROJECT_USERNAME
    - brew update
    - brew prune
    - brew upgrade go || brew install go
    - brew upgrade git || brew install git
  override:
    - script/bootstrap

test:
  override:
    - script/cibuild
